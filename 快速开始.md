# 快速运行指南

## ✅ 项目已经正常运行！

Mock Vehicle API 正在运行中：http://127.0.0.1:8765

---

## 🎯 三种演示方式

### 方式1: Python演示脚本（推荐）

```bash
# 直接运行
python demo.py
```

**这个脚本会展示**：
- ✓ 5个演示场景
- ✓ 意图识别结果
- ✓ 实体抽取
- ✓ 任务规划
- ✓ 槽位管理

---

### 方式2: 单个命令测试

```bash
# 测试1: 简单查询
python -c "from minicoder.cli import main; import sys; sys.argv = ['minicoder', '--analyze-intent', '查询A102的续航']; main()"

# 测试2: 槽位缺失
python -c "from minicoder.cli import main; import sys; sys.argv = ['minicoder', '--analyze-intent', '明天去上海，需要充电吗']; main()"

# 测试3: 完整规划
python -c "from minicoder.cli import main; import sys; sys.argv = ['minicoder', '--analyze-intent', 'A102明早8点去上海虹桥站，判断是否需要充电']; main()"

# 测试4: 知识查询
python -c "from minicoder.cli import main; import sys; sys.argv = ['minicoder', '--analyze-intent', '冬天如何保养电池']; main()"

# 测试5: 空调控制
python -c "from minicoder.cli import main; import sys; sys.argv = ['minicoder', '--analyze-intent', '把A102的空调设为24度']; main()"
```

---

### 方式3: Mock API测试

```bash
# 浏览器打开
http://127.0.0.1:8765/docs

# 或命令行测试
curl -H "Authorization: Bearer local-demo-token" http://127.0.0.1:8765/v1/vehicles/A102/status
```

---

## 📊 运行结果说明

### 意图识别输出示例
```json
{
  "understanding": {
    "intent": "range_query",           // 意图类型
    "status": "ready",                 // 状态（ready/missing_info）
    "confidence": "high",              // 置信度
    "entities": {                      // 提取的实体
      "vehicle_id": "A102"
    },
    "missing_fields": []               // 缺失字段（空=完整）
  },
  "plan": {
    "status": "ready",
    "steps": [                         // 执行计划
      {
        "id": "vehicle_status",
        "action": "get_vehicle_status",
        "arguments": {"vehicle_id": "A102"}
      }
    ]
  }
}
```

---

## 🎬 面试演示流程（3分钟）

1. **运行演示脚本**（1分钟）
   ```bash
   python demo.py
   ```
   展示5个场景的输出

2. **打开API文档**（30秒）
   浏览器访问：http://127.0.0.1:8765/docs
   试用一个端点

3. **讲解架构**（1.5分钟）
   - 五层架构设计
   - 核心技术亮点（槽位补全、$ref引用、并发优化）
   - 性能指标（7,773行代码、86.47%覆盖率）

---

## ❓ 为什么不能进入交互式REPL？

**原因**：需要配置真实的OpenAI API Key

**解决方案**：
1. **演示模式**（当前）- 无需API Key，展示所有核心功能 ✅
2. **本地模型**（Ollama）- 免费，支持完整对话
3. **云端API**（DeepSeek/OpenAI）- 付费，最佳体验

**对于面试演示**：当前的演示模式已经完全足够！

---

## 📁 重要文件

| 文件 | 说明 |
|-----|------|
| `demo.py` | Python演示脚本（推荐使用）|
| `项目运行说明.md` | 完整运行指南 |
| `docs/简历项目文案.md` | STAR简历文案 |
| `docs/快速参考手册.md` | 面试准备手册 |

---

## 🎉 总结

你的项目完全可以运行！

- ✅ Mock API正常运行
- ✅ 意图识别正常工作
- ✅ 任务规划正常生成
- ✅ 演示脚本ready

**立即开始**：运行 `python demo.py` 查看效果！