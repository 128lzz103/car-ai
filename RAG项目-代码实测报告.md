# zimuRAG 项目实测报告（代码级核对）

> 生成时间：2026-09-11
> 核对方式：从 https://github.com/128lzz103/zimuRAG.git 克隆原始版本，与本地 E:\rag项目\zimuRAG-main\zimuRAG-main 逐文件对比，并实跑测试套件。

---

## 一、结论速览

| 问题 | 结论 |
|------|------|
| 本地这份是不是被你改烂了？ | **没有。源码与原版零差异**，唯一区别是换行符（LF → CRLF）和你自己加的 `.env` |
| 项目能不能跑？ | **能跑。113 个测试通过**，2 个失败，2 个跳过 |
| 那 2 个失败是谁的锅？ | **项目本身的既有缺陷**，不是环境问题，不是你的改动 |
| 简历上写的混合检索呢？ | **原始项目里就没有**。原作者的 DESIGN.md 写了计划，代码里从未实现 |

---

## 二、版本比对（本地 vs GitHub 原版）

比对方式：`git diff --no-index --ignore-all-space`，并逐文件算 MD5。

- 原仓库提交数：1 个（`9304e31 feat: zimuRAG v1.0 - MVP release`）
- 原仓库文件数 85，本地 86（多出来的就是 `.env`）
- **`app/` 目录：源码零差异**
- **`tests/` 目录：源码零差异**
- 唯一差异：`.pyc` 缓存文件，以及换行符（原版 LF，本地 CRLF）

**为什么一开始文件哈希全不一样？**
本地这份是从 zip 解压的，Windows 下换行符变成了 CRLF，所以每个文件的 MD5 都变了。忽略空白后，一个字节都不差。

---

## 三、测试实跑结果

```
python -m pytest -q
→ 113 passed, 2 failed, 2 skipped in 47.56s
```

### 失败的两个

1. `tests/test_admin.py::test_ingest_document_records_failure_reason`
2. `tests/test_ingest_documents.py::test_ingest_document_rejects_unknown_extension`

### 根因

两个测试都往 `ingest_document` 塞了一个伪装的 docx（内容只有 `PK\x03\x04` 四个字节，是个坏 zip）。

- 测试期望：抛出 / 记录「不支持的文件类型」
- 实际发生：`zipfile.BadZipFile: File is not a zip file`

也就是说，`app/services/ingest.py` 和 `app/services/parsing.py` 的异常处理**只覆盖了 `ValueError`，没覆盖解析器抛出的 `BadZipFile`**。

后果：真实场景里用户上传一个损坏的 Word 文件，服务端不是返回「文件损坏」的友好提示，而是抛未捕获异常 → 500。

**这是个真 bug，值得修，而且修完是很漂亮的面试素材。**

---

## 四、简历与实测的差距（重点）

| 简历/手册上的说法 | 代码实测 | 风险 |
|---|---|---|
| 混合检索：Dense + BM25 + RRF 融合 | ❌ 代码里搜不到 `bm25` / `RRF` / `sparse`，**纯 dense 向量检索** | 🔴 高，一问融合权重就露 |
| Top-K 准确率提升 75%、查询改善率 60% | ❌ 项目里没有任何评测脚本能算出这两个数 | 🔴 高，无数据支撑 |
| 向量库用 Qdrant | ⚠️ 代码写了 `app/providers/qdrant.py`，但 `.env` 里 `VECTOR_STORE_PROVIDER=fake`，真实向量库从未验证 | 🟠 中，让演示就露 |
| 50-100 份文档、200-500 页 | ⚠️ 实际测试集是 1 份 docx（344 段落），20 道测试题 | 🟠 中 |
| Provider 抽象层（6 接口 + 假实现） | ✅ **真的**，`app/providers/base.py` 定义 6 个抽象接口，真假两套实现 | 🟢 可以放心讲 |
| 摘要分层两跳检索 | ✅ **真的**，`answer.py` 先查 `is_summary=True` 锁定章节，再在章节内精检，无摘要时自动退化为全量检索 | 🟢 亮点，可以讲深 |
| 云 Rerank 重排 | ✅ **真的**，`DashScopeReranker` 走 text-rerank 原生端点，`answer.py` 确实调用 | 🟢 可以放心讲 |
| 问题按需改写 | ✅ **真的** | 🟢 |
| 多域软路由 | ✅ **真的**，`_search` 逐域两跳后合并，按得分交 rerank | 🟢 |
| 企微 WebSocket 长连接 | ✅ **真的**，`app/bot/wecom.py` | 🟢 未实测连通 |
| 设计文档里的 DDD 术语（限界上下文、聚合根、事件溯源） | ⚠️ 代码里没有对应实现，属堆术语 | 🟠 中，容易被问穿 |

---

## 五、建议动作（按性价比排序）

1. **修那 2 个失败测试**（半天）—— 让 `pytest` 全绿。面试官真的会跑测试，全绿和「2 failed」是两个印象。
2. **把混合检索真写出来**（2-3 天）—— BM25 + RRF 融合，架构早就为它留好了位置（`_search` 里加一路稀疏召回即可）。写完简历那句话就成真了，而且你能讲实现细节。
3. **建评测脚本**（半天-1 天）—— 手里已有 20 道题和标准答案，跑出真实的 Recall@3，把「75%」换成真数字。
4. **改简历措辞**（10 分钟）—— 如果不想写代码，就把「混合检索」拿掉，只写「稠密向量检索 + 摘要分层定位 + 云 Rerank 重排」。每个字都是真的，还能讲深。
5. **删掉堆术语的部分** —— 限界上下文、聚合根、事件溯源这些词，代码里没有对应物，删了更安全。

---

## 六、可以放心讲的真亮点（背这些，不用编）

1. **Provider 接缝设计**：6 个外部依赖（LLM / Embedding / Rerank / VectorStore / OCR / Summarizer）全部抽象成接口，业务代码零厂商依赖；测试注入内存假实现，**117 个测试零成本、零网络、确定性**。
2. **摘要分层两跳检索**：入库时为每个章节生成摘要向量（`is_summary=True` + `summary_of`），检索第一跳在摘要层锁定 top-3 章节，第二跳限定章节内精检 chunk；**无摘要时自动退化全量检索，保证不漏召回**。这个降级设计是加分项。
3. **按需问题改写**：不是每次都改写，只在含指代/省略/过短时改，省延迟省成本。
4. **多域软路由**：域间结果合并不硬排除，避免路由错了就直接捞不到。
5. **明确的工程取舍**：为什么 Qdrant 不用 Milvus、为什么一期云 API 三期本地化，这些权衡讲得清楚。

---

## 七、待办

- [ ] 修 `BadZipFile` 未捕获导致的 2 个失败测试
- [ ] 决定：自己实现混合检索，还是改简历措辞
- [ ] 建评测脚本，产出真实 Recall@3
- [ ] 用 fake provider 全链路演示一次（不花钱、不联网），练熟讲解词
