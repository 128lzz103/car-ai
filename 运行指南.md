# 项目运行问题排查与解决方案

## ✅ 当前运行状态

```
✓ Mock Vehicle API: 正常运行 (http://127.0.0.1:8765)
✓ minicoder 模块: 可正常导入
✓ 意图识别: 规则模式正常工作
✓ 配置文件: .env 已创建
```

## ⚠️ 核心问题：需要 API Key

项目的 **意图识别和对话理解功能** 可以**完全离线运行**（使用规则模式），但如果需要完整的 LLM 对话功能，需要配置 API Key。

---

## 🎯 三种解决方案

### 方案1: 演示模式（推荐，无需API Key）✅

**适用场景**: 面试演示、快速展示项目能力

#### Windows 快速演示
```cmd
# 直接双击运行
demo.bat

# 或命令行运行
demo.bat
```

#### 单个命令演示
```bash
# 演示1: 简单查询
python -c "from minicoder.cli import main; import sys; sys.argv = ['minicoder', '--analyze-intent', '查询A102的续航']; main()"

# 演示2: 槽位识别
python -c "from minicoder.cli import main; import sys; sys.argv = ['minicoder', '--analyze-intent', '明天去上海，需要充电吗']; main()"

# 演示3: 完整行程规划
python -c "from minicoder.cli import main; import sys; sys.argv = ['minicoder', '--analyze-intent', 'A102明早8点去上海虹桥站，判断是否需要充电']; main()"
```

**说明**:
- ✅ 展示意图识别（7种车载意图）
- ✅ 展示实体抽取（车辆ID、地点、时间）
- ✅ 展示任务规划（六步行程规划）
- ✅ 展示槽位管理（缺失字段识别）
- ❌ 无法进行完整的对话交互

---

### 方案2: 使用免费的 Ollama（推荐）✅

**适用场景**: 完整功能测试，不想付费

#### 步骤1: 安装 Ollama
```bash
# 下载地址
https://ollama.com/download

# Windows: 下载 .exe 安装包，双击安装
```

#### 步骤2: 下载模型
```bash
# 打开命令行，拉取中文模型
ollama pull qwen2.5:7b

# 等待下载完成（约4GB）
```

#### 步骤3: 修改配置
编辑 `e:\车载ai\.env` 文件，修改以下内容：
```bash
MINICODER_PROVIDER=openai
OPENAI_API_KEY=ollama
OPENAI_BASE_URL=http://localhost:11434/v1
MINICODER_MODEL=qwen2.5:7b
```

#### 步骤4: 运行完整对话
```bash
# 启动交互式REPL
python -c "from minicoder.cli import main; main()" --profile automotive

# 或简单测试
python -c "from minicoder.cli import main; import sys; sys.argv = ['minicoder', '-p', '查询A102的续航']; main()"
```

**优点**:
- ✅ 完全免费
- ✅ 数据不离开本地
- ✅ 支持完整对话

**缺点**:
- ❌ 需要下载模型（4-8GB）
- ❌ 响应速度较慢
- ❌ 准确率不如云端模型

---

### 方案3: 使用云端API（最佳体验）💰

#### 选项A: DeepSeek（性价比最高）
```bash
# 注册: https://platform.deepseek.com/
# 获取API Key

# 编辑 .env
MINICODER_PROVIDER=openai
OPENAI_API_KEY=sk-你的DeepSeek-API-Key
OPENAI_BASE_URL=https://api.deepseek.com/v1
MINICODER_MODEL=deepseek-chat

# 费用: ¥0.001/1K tokens (约为OpenAI的1/100)
```

#### 选项B: Kimi（国内稳定）
```bash
# 注册: https://platform.moonshot.cn/
# 获取API Key

# 编辑 .env
MINICODER_PROVIDER=openai
OPENAI_API_KEY=你的Kimi-API-Key
OPENAI_BASE_URL=https://api.moonshot.cn/v1
MINICODER_MODEL=moonshot-v1-8k
```

#### 选项C: OpenAI（最强能力）
```bash
# 需要国际信用卡

# 编辑 .env
MINICODER_PROVIDER=openai
OPENAI_API_KEY=sk-你的OpenAI-Key
OPENAI_BASE_URL=https://api.openai.com/v1
MINICODER_MODEL=gpt-4o

# 费用: $2.50/1M input tokens
```

---

## 🎬 快速演示指南（面试用）

### 演示场景1: 意图识别能力（30秒）

```bash
# 运行演示脚本
demo.bat

# 输出将展示：
# - 意图识别: range_query
# - 实体提取: {"vehicle_id": "A102"}
# - 任务规划: 1步（get_vehicle_status）
```

**演示话术**:
> "这是意图识别模块，我输入'查询A102的续航'，系统自动识别出意图类型是range_query，提取出车辆ID是A102，并生成了一个执行步骤：调用get_vehicle_status API。整个过程完全基于规则，无需调用LLM，响应速度在10ms以内。"

### 演示场景2: 槽位补全识别（45秒）

```bash
# 运行
python -c "from minicoder.cli import main; import sys; sys.argv = ['minicoder', '--analyze-intent', '明天去上海，需要充电吗']; main()"

# 输出将展示：
# - missing_fields: ["vehicle_id", "departure_time"]
# - status: missing_info
```

**演示话术**:
> "这里展示槽位管理。用户说'明天去上海，需要充电吗'，系统识别出这是一个行程规划意图，但缺少车辆ID和出发时间，状态被标记为missing_info，会触发3轮补全策略，自动追问缺失字段。"

### 演示场景3: 复杂任务规划（1分钟）

```bash
# 运行
python -c "from minicoder.cli import main; import sys; sys.argv = ['minicoder', '--analyze-intent', 'A102明早8点去上海虹桥站，判断是否需要充电']; main()"

# 输出将展示：
# - 6步执行计划（vehicle_status → route → energy_calc → charging_stations → advice → knowledge）
# - 依赖关系（depends_on）
# - $ref安全引用
```

**演示话术**:
> "这是完整的六步行程充电规划。系统自动生成了：查车辆状态、估算路线、计算能耗、查充电站、生成建议、检索知识。每个步骤都有明确的依赖关系，比如Step3依赖Step1和Step2的输出，通过$ref机制安全引用前序结果。整个规划是确定性的，零额外LLM调用。"

### 演示场景4: Mock API调用（30秒）

```bash
# 在浏览器打开
http://127.0.0.1:8765/docs

# 或命令行测试
curl -H "Authorization: Bearer local-demo-token" http://127.0.0.1:8765/v1/vehicles/A102/status
```

**演示话术**:
> "这是我实现的Mock Vehicle API，基于FastAPI。提供5个RESTful端点：车辆状态、路线估算、充电站查询、空调控制。包含线程安全的状态管理、三级权限控制、故障注入测试。可以打开Swagger文档交互式测试。"

---

## 📋 常见问题

### Q1: 为什么不能进入交互式REPL？
**A**: 需要配置有效的API Key。可以：
- 使用 `--analyze-intent` 模式演示（无需API Key）
- 安装 Ollama 使用本地模型
- 购买云端API服务

### Q2: 如何快速展示项目功能？
**A**: 运行 `demo.bat` 演示脚本，展示所有核心能力，无需API Key。

### Q3: Ollama 下载太慢怎么办？
**A**: 可以使用国内镜像，或直接使用演示模式。

### Q4: 有没有完全离线的运行方式？
**A**: 有，使用规则模式的意图识别：
```bash
python -c "from minicoder.cli import main; import sys; sys.argv = ['minicoder', '--analyze-intent', '你的输入']; main()"
```

### Q5: 面试时如何演示？
**A**: 
1. 打开 `http://127.0.0.1:8765/docs` 展示Mock API
2. 运行 `demo.bat` 展示意图识别
3. 讲解五层架构和技术难点
4. 展示代码结构和测试覆盖率

---

## 🔍 运行状态检查命令

```bash
# 检查Mock API
curl http://127.0.0.1:8765/v1/health

# 检查minicoder导入
python -c "from minicoder.cli import main; print('OK')"

# 检查配置文件
cat .env

# 检查Ollama（如已安装）
curl http://localhost:11434/api/tags

# 运行测试
pytest
```

---

## 🎯 推荐方案总结

| 场景 | 推荐方案 | 命令 |
|-----|---------|------|
| **面试演示** | 演示模式 | `demo.bat` |
| **快速测试** | 演示模式 | `--analyze-intent` |
| **完整功能** | Ollama | 安装后配置.env |
| **生产级** | DeepSeek/Kimi | 购买API Key |

---

## 📞 需要帮助？

如果还有运行问题，请提供：
1. 具体的错误信息
2. 运行的完整命令
3. Python版本和系统信息

我会帮你快速定位问题！